# Creates a directory for every month with full month name

## Command:
```
$ IFS=';'; set -f; mkdir -- $(locale mon)
```

## Explanation:
Explanation
IFS=';'; set -f; mkdir -- $(locale mon)

## Limitations:
No limitations specified

